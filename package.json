{
  "name": "sius-system-definitions",
  "version": "1.0.20",
  "description": "type definitions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.18",
    "rimraf": "^3.0.2",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "include": [
    "src"
  ],
  "files": [
    "dist"
  ],
  "dependencies": {
    "sius-core-types": "^1.0.19"
  }
}
